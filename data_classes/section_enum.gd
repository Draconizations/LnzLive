class_name Section
extends Node
## section_enum.gd
## Defines an enumeration for the different sections of a LNZ document
## NOTE: I don't know that this gets used anymore, check if can delete

enum Section {
	BALL,
	MOVE,
	PROJECT,
	LINE
}
